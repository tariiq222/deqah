---
"admin": patch
---

Add official Deqah brand logo (دقة wordmark + blue check) and matching favicon to the super-admin shell + login card. Includes Next.js auto-discovered `app/icon.svg` + `app/apple-icon.svg`, plus dark/white logo variants under `public/logos/`.
